import FeaturedJourneysClient from './FeaturedJourneysClient';

export default function FeaturedJourneys({ packages }: { packages: any[] }) {
    return <FeaturedJourneysClient packages={packages} />;
}
